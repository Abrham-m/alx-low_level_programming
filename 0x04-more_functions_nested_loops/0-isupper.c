#include "main.h"

/**
 * _isupper - is a function to check upper case
 * @c: is a character input
 * Return: 1 for uppercase or 0 for lowecase
 */

int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
