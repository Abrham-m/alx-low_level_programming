#include "main.h"

/**
 * _strncpy - is a function
 * @dest: is a varible
 * @src: is a function
 * @n: is a number
 * Return: dest
 */

char *_strncpy(char *dest, char *src, int n)
{
	strncpy(dest, src, n);
	return (dest);
}
