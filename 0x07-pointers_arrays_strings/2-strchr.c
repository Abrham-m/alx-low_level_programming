#include "main.h"

/**
 * _strchr - is a fuction that search
 * @s: pointer
 * @c: character
 * a_character and make the pointer
 * points to point to the location
 * Return: 0
 */

char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
