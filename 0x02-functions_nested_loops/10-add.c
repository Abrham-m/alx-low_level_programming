#include "main.h"

/**
 * add - it adds two number and print the result
 * @a: it is one variable
 * @b: it is the second variable
 * Return: it returns value of result
*/

int add(int a, int b)
{
	int result;

	result = a + b;

	return (result);
}
