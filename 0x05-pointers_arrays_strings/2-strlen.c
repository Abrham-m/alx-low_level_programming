#include "main.h"

/**
 * _strlen  - is afunction that returns the length of a string
 * @s: is avariable
 * Return: tha value of len variable
 */

int _strlen(char *s)
{
	int len;

	len = strlen(*s);

	return (len);
}
