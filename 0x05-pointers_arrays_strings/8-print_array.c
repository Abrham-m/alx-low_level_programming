#include "main.h"

/**
 * print_array- is
 * @a: is avvariable
 * @n: is seconfd varible
 */

void print_array(int *a, int n)
{
	int c;

	for (c = 0; c < n; c++)
	{
		printf("%d", a[i]);
		if (c != n - 1)
		{
			printf(",");
			printf(" ");
		}
	}
	printf("\n");
}

